# Example kit

This folder demonstrates the layout: add scan files (PDF, PNG, etc.) beside this file. **Source of truth** for catalog fields is [`metadata.json`](metadata.json) in this folder. Root `kits.json` is generated from every kit’s `metadata.json`.
